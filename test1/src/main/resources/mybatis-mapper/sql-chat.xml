<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.ChatMapper">
	<insert id="insertChatMessage" parameterType="hashmap">
		INSERT INTO CHAT_MESSAGE 
		(CHAT_ID, CDATE, MESSAGE, SENDER_ID, CHATROOM_NO)
		VALUES 
		(CHAT_MESSAGE_SEQ.NEXTVAL, SYSDATE, #{message}, #{senderId}, #{chatroomNo})
	</insert>
</mapper>

