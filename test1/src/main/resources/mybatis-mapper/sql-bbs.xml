<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.BbsMapper">

	<select id="selectBbsList_bak" parameterType="hashmap" 
			resultType="com.example.test1.model.Bbs">
		select * 
		from TBL_BBS
		ORDER BY BBS_NUM DESC
	</select>
	
	<select id="selectBbsList" parameterType="hashmap" 
			resultType="com.example.test1.model.Bbs">
		SELECT * 
		from TBL_BBS
		where 1=1
		<if test = "searchOption == 'all'">
			AND (TITLE LIKE '%'||#{keyword} ||'%' OR
			USERID LIKE '%'||#{keyword} || '%'
		)
		</if>
		<if test = "searchOption == 'title'">
			AND TITLE LIKE '%'||#{keyword} ||'%'
		</if>
		<if test = "searchOption == 'iddd'">
			AND USERID LIKE '%'||#{keyword} ||'%'
		</if>
		ORDER BY BBS_NUM DESC
		OFFSET #{page} ROW FETCH NEXT #{pageSize} ROWS ONLY
	</select>
	
	<select id="selectBbsCnt" parameterType="hashmap" resultType="int">
        SELECT COUNT(*)
		FROM TBL_BBS B
		WHERE 1=1	
		<if test = "searchOption == 'all'">
			AND (TITLE LIKE '%'||#{keyword} ||'%' OR
			USERID LIKE '%'||#{keyword} || '%'
			)
		</if>
		<if test = "searchOption == 'title'">
			AND TITLE LIKE '%'||#{keyword} ||'%'
		</if>
		<if test = "searchOption == 'iddd'">
			AND USERID LIKE '%'||#{keyword} ||'%'
		</if>
	</select>
	
	
	
	
	
	<insert id = "insertBbs" 
			parameterType="hashmap" 
			useGeneratedKeys="true" 
			keyColumn="BBS_NUM" 
			keyProperty="bbsNum">
		INSERT INTO TBL_BBS 
		VALUES (
		        BBS_SEQ.NEXTVAL, 
		        #{title}, 
		        #{contents}, 
		        0, 
		        #{userId},
		        SYSDATE, 
		        SYSDATE 
		        )
	</insert>
	
	<delete id="deleteBbs" parameterType="hashmap">
		DELETE FROM TBL_BBS
		WHERE BBS_NUM = #{selectItem}
	</delete>
	
	<delete id="deleteBbsList" parameterType="hashmap">
		DELETE FROM TBL_BBS
		WHERE BBS_NUM = #{selectItem}
	</delete>

	
	
	<select id="selectBbs" parameterType="hashmap" resultType="com.example.test1.model.Bbs">
		SELECT B.*
		FROM TBL_BBS B
		WHERE B.BBS_NUM = #{bbsNum}
	</select>
	
	
	
	<update id="updateBbs" parameterType="hashmap">
		UPDATE TBL_BBS 
		SET TITLE = #{title}, CONTENTS = #{contents}
		WHERE BBS_NUM = #{bbsNum}
	</update>
	
	
	<insert id = "insertBbsImg" parameterType="hashmap">
		INSERT INTO TBL_BOARD_FILE
		VALUES(FILE_SEQ.NEXTVAL, #{bbsNum}, #{path}, #{filename}, #{orgName}, #{size, javaType=long, jdbcType=NUMERIC}, #{ext})
	</insert>
	
	<select id="selectFileList" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT *
		FROM TBL_BOARD_FILE
		WHERE BOARDNO = #{bbsNum}
	</select>
</mapper>

