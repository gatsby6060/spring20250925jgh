<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.test1.mapper.BoardMapper">

	<select id="selectBoardList" parameterType="hashmap" resultType="com.example.test1.model.Board">
		SELECT
		*
		FROM TBL_BOARD
		ORDER BY BOARDNO DESC
	</select>
	
	<!--
		[게시글 삭제 - boaDel]
		- BoaMapper.boaDel(int boardno) 시그니처에 맞춰 parameterType="int" 사용
		- 단일 기본형 파라미터는 #{value} 로 접근합니다.
	-->
	<delete id="boaDel" parameterType="int" >
		DELETE FROM TBL_BOARD
		WHERE BOARDNO = #{value}
	</delete>
	<!--
	<delete id="boaDel" parameterType="int" resultType="int">
		delete 
		FROM BOARD 
		WHERE BOARDNO = #{keyword}
	</delete>
	-->
</mapper>




